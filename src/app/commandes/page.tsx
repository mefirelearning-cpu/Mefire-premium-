import {createOrderAction} from '@/app/actions/crm';
export default function Commandes(){const stages=[['Nouvelles','0'],['Paiement en attente','0'],['À activer','0'],['Actives','0']];return <><div className="top"><div><h1>Commandes</h1><div className="muted">Pipeline commercial de la demande jusqu’à l’activation.</div></div></div><section className="grid">{stages.map(([l,v])=><div className="card" key={l}><div className="muted">{l}</div><div className="value">{v}</div></div>)}</section><section className="cols"><form className="card" action={createOrderAction}><h2>+ Nouvelle commande</h2><label>ID client *</label><input name="customerId" required placeholder="UUID client"/><label>Montant total (FCFA) *</label><input name="total" required inputMode="numeric" placeholder="5000"/><button type="submit">Créer la commande</button></form><div className="card"><h2>Dernières commandes</h2><p className="muted">La liste sera alimentée automatiquement depuis PostgreSQL.</p></div></section></>}